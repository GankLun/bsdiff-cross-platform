apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}
jar {
    manifest {
        attributes 'Main-Class': 'com.cross.diff.Main'
        attributes 'Class-Path': 'diffbinary2.jar'
    }
}